export class AppConfig {
  public static APP_PROTOCOL = 'http://';
  public static APP_API_DOMAIN = 'demo.hey-community.com';
  public static APP_ASSET_DOMAIN = 'public.hey-community.cn';

  public static WEBAPP_API_DOMAIN = null // It's no need to set, the default for the user to visit to domain
  public static WEBAPP_ASSET_DOMAIN = null // It's no need to set, the default for the user to visit to domain
  public static WEBAPP_CDN_ASSET_DOMAIN = '';
}

